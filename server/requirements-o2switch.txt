# =============================================================================
# SIREN — Serveur / Backend — Dépendances pour o2switch MUTUALISÉ
# =============================================================================
# Même stack que requirements.txt, débarrassée des dépendances inutiles/lourdes
# sur un hébergement mutualisé :
#   - osmnx / networkx / shapely : jamais importés dans le code (retirés)
#   - pandas / scipy / tqdm       : non importés (scipy revient via scikit-learn)
#   - uvicorn / gunicorn          : remplacés par Passenger (WSGI)
#   - alembic / psycopg2          : schéma appliqué via Supabase (db_schema.sql)
#   - + a2wsgi                    : pont ASGI -> WSGI pour Passenger
# Installation : pip install -r requirements-o2switch.txt
# =============================================================================

# --- Pont WSGI (obligatoire pour Passenger) ---
a2wsgi>=1.10.0,<2

# --- Framework API ---
fastapi==0.115.0

# --- Base de données (Supabase = PostgreSQL externe + PostGIS) ---
sqlalchemy==2.0.35
asyncpg==0.30.0
geoalchemy2==0.15.1

# --- Validation ---
pydantic==2.9.0
pydantic-settings==2.5.0
email-validator==2.2.0

# --- Authentification ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.12
pyotp==2.9.0

# --- Tâches : Celery conservé pour les décorateurs, mais exécution via cron ---
celery[redis]==5.4.0
redis==5.1.0

# --- Temps réel (dev uniquement, WebSocket inactif via WSGI) ---
websockets==13.0

# --- Push notifications ---
firebase-admin==6.5.0

# --- IA / ML (scikit-learn importé à la volée, dans le runner cron uniquement) ---
scikit-learn==1.5.1
numpy==1.26.4

# --- HTTP Client ---
httpx==0.27.2

# --- Logging / Monitoring ---
sentry-sdk==2.13.0
structlog==24.4.0

# --- Utilitaires ---
python-dateutil==2.9.0
tenacity==9.0.0